// RAG config for embedding models per file type

export const ragConfig = {
  default: 'text-embedding-3-small',
  pdf: 'text-embedding-3-small',
  docx: 'text-embedding-3-small',
  csv: 'text-embedding-3-small',
  // Add more file types and models as needed
}; 